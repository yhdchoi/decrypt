package com.yhdc.backendapi.service;

public interface MemberServiceImpl {

//	default Member dtoToEntity(MemberDTO memberDTO) {
//
//		Member member = Member.builder().id(memberDTO.getId()).username(memberDTO.getUsername())
//				.email(memberDTO.getEmail()).lastname(memberDTO.getLastname()).firstname(memberDTO.getFirstname())
//				.role(memberDTO.getRole()).boards(memberDTO.getBid()).regDate(memberDTO.getRegDate())
//				.modDate(memberDTO.getModDate()).build();
//
//		return member;
//	}
	
//	default MemberDTO entityToDTO(Member member) {
//		
//		MemberDTO dto = MemberDTO.builder().id(member.getId()).build();
//		
//		return dto;
//	}

}
